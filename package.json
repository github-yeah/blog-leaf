{
  "name": "blog.leaf",
  "version": "1.0.0",
  "description": "Vue + VuePress + TypeScript + JavaScript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "vuepress dev docs . --open --host \"localhost\"",
    "docs:build": "vuepress build docs ."
  },
  "author": "yxh",
  "license": "ISC",
  "dependencies": {
    "vuepress": "^1.8.0"
  }
}
